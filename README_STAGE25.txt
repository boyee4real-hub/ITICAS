ITICAS Stage 25 — Context-Aware Predictive Intelligence & Explainability

Overlay destination: C:\iticas

Preserves .venv, .env, local database, Supabase archive, users, monitoring locations and accumulated traffic history.

After extraction:
  cd /d C:\iticas
  call .venv\Scripts\activate
  APPLY_STAGE25.cmd
  RUN_APP.cmd

Then open /predictions and press Ctrl+F5 once.
